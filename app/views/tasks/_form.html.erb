<%= form_with(model: @task, url: (@task.new_record? ? circle_tasks_path : circle_task_path)) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.datetime_select :expiration_date %>
  </div>

  <div class="field">
    <%= form.label :completed %>
    <%= form.check_box :completed %>
  </div>

  <div class="field">
    <%= form.label :completed_by_id %>
    <%= form.number_field :completed_by_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :created_by_id %>
    <%= form.number_field :created_by_id %>
  </div>

  <div class="field">
    <%= form.label :task_generator_id %>
    <%= form.text_field :task_generator_id %>
  </div>

  <div class="field">
    <%= form.label :circle_id %>
    <%= form.text_field :circle_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
